# spring-cache-redis
This project is to implement basic caching using spring redis on a my sql db which can prevent transaction count on mysql which improves performance

pre requisite:
redis cli
refer to https://redis.io/download for latest cli setup and installation
reference https://redis.io/commands for executing commands